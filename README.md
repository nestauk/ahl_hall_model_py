# `bw` Hall model Python wrapper

A Python wrapper for the R `bw` package Hall model C++ implementation.

Specifically, this package provides Python bindings via shims for the `adult_weight` and `energy_build` functions.
